server:
  port: 8001

spring:
  application:
    name: ssgnack

  # driver config
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://ssg-java2.iptime.org:3306/ssgdb
    username: ssg
    password: nack
  thymeleaf:
    cache: false

# mybatis config
mybatis:
  mapper-locations: classpath:mappers/*.xml